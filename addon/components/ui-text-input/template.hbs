{{input
  id=@inputId
  value=@value
  required=@required
  type=@type
  placeholder=@placeholder
  disabled=this.isDisabled
  max=this.max
  min=this.min
  key-up=(action "onKeyUp")
  focus-out=(action "didFocusOut")
}}